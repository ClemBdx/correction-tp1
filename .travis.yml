language: java

addons:
  sonarcloud:
    organization: "clembdx"
    token: "$SONARCLOUD_TOKEN"

jobs:
  include:
    - stage: dev
      name: Test 1
      script: bash scripts/build.sh
    - stage: prod
      name: Test 2
      script: bash scripts/docker.sh

branches:
  only:
    - develop

cache:
  directories:
    - $HOME/.m2

services:
  - docker

